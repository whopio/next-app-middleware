{
  "name": "middleware-next",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "bundle": "tsup ./src/index.ts --format cjs --dts",
    "build": "concurrently \"pnpm run transpile\" \"pnpm run types\"",
    "transpile": "swc src/**/*.ts src/*.ts -d ./ -s -C module.type=commonjs",
    "types": "tsc --emitDeclarationOnly",
    "dev": "concurrently \"pnpm run transpile --watch\" \"pnpm run types --watch\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@swc/cli": "^0.1.57",
    "@swc/core": "^1.3.21",
    "concurrently": "^7.6.0",
    "next": "13.0.6",
    "tsconfig": "workspace:^0.0.0",
    "typescript": "^4.9.3"
  },
  "peerDependencies": {
    "next": "13.0.6"
  },
  "dependencies": {
    "@middleware-next/codegen": "workspace:^1.0.0",
    "@middleware-next/runtime": "workspace:^1.0.0"
  }
}
